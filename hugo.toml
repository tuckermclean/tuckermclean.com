baseURL = 'https://tuckermclean.com/'
languageCode = 'en-us'
title = 'Tucker McLean — Infrastructure Architect & Platform Engineer'
uglyURLs = true
publishDir = 'dist'
disableKinds = ['taxonomy', 'term', 'section']

[params]
  author = "Tucker McLean"
  initials = "TM"
  email = "me@tuckermclean.com"
  phone = "+18124304200"
  phoneDisplay = "812-430-4200"
  location = "Beaverton, Oregon"
  domain = "tuckermclean.com"
  linkedin = "tucker-mclean"
  github = "tuckermclean"
  role = "Infrastructure Architect & Platform Engineer"

[markup.goldmark.renderer]
  unsafe = true

[permalinks]
  posts = '/posts/:contentbasename'

[server]
  [[server.headers]]
    for = '/**'
    [server.headers.values]
      X-Frame-Options = 'SAMEORIGIN'
      X-Content-Type-Options = 'nosniff'
      X-XSS-Protection = '1; mode=block'
